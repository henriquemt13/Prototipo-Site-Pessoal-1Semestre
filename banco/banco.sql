create database AA;
use AA;

create table Cadastrar(
id int primary key identity(1,1),
nome varchar(60),
telefone varchar(14),
email varchar (70),
senha varchar(30),
endereco varchar(70),
cidade varchar(60),
estado varchar(2),
cep varchar(9)
b
);

